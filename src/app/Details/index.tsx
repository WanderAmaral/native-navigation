import { View, Text } from "react-native";
import { styles } from "./style";

export function Details() {
  return (
    <View style={styles.container}>
      <Text style={{color: 'white'}}>Wandao rei delas</Text>
    </View>
  );
}
